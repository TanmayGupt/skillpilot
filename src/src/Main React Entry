/* STREAMING_CHUNK:Initializing React DOM root... */
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
import './index.css';

/* STREAMING_CHUNK:Rendering main SkillPilot application... */
ReactDOM.createRoot(document.getElementById('root')).render(
<React.StrictMode>

</React.StrictMode>,
);
